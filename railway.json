{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm run build"
  },
  "deploy": {
    "startCommand": "sh -c 'PORT=8080 HOST=0.0.0.0 node ./dist/server/entry.mjs & PORT=3000 HOST=0.0.0.0 node ./dist/server/entry.mjs & wait'",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
